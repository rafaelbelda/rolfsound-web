# Handoff bundles do Claude Design (26+ MB, fonte fica no claude.ai/design)
*-handoff.zip

# Python (backend FastAPI)
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Runtime do backend (criados pelo app)
control.log*
db/*.db
music/
cache/
config.json

# Binário do extrator Essentia — tools/setup_essentia.py baixa por plataforma
tools/essentia/

# Node (se entrar tooling de frontend no futuro)
node_modules/

# Claude Code — permissões locais da máquina
.claude/settings.local.json
CLAUDE.md

# SO / editor
.DS_Store
Thumbs.db
desktop.ini
